name: trigger
on:
  pull_request:

jobs:
  artifact_1:
    runs-on: ubuntu-latest
    steps:
      - run: | 
          echo "id" > test.sh
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: my-artifact
          path: test.sh
